<Page x:Class="WeSplit.AddJourneyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WeSplit"
      mc:Ignorable="d" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="Segoe UI"
      Loaded="AddJourneyPage_Loaded"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddJourneyPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="19*"/>
            <RowDefinition Height="206*"/>
        </Grid.RowDefinitions>
        <DockPanel LastChildFill="False" Grid.Row="0">
            <Button x:Name="GoBackBtn" Content="Quay lại" Click="GoBackBtn_Click" DockPanel.Dock="Left"/>
            <Button x:Name="SaveBtn" Content="Lưu" Click="SaveBtn_Click" DockPanel.Dock="Right"/>
        </DockPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="123*"/>
                <ColumnDefinition Width="277*"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="6*"/>
                    <RowDefinition Height="25*"/>
                    <RowDefinition Height="31*"/>
                    <RowDefinition Height="41*"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0" x:Name="JourneyNameTextBox" materialDesign:HintAssist.Hint="Tên chuyến đi"/>
                <DockPanel Grid.Row="1">
                    <Button x:Name="AddDestinationBtn" Content="Thêm địa danh" DockPanel.Dock="Bottom" Click="AddDestinationBtn_Click"/>
                    <ListView x:Name="DestinationListView">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DesName}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DockPanel>
                <DockPanel LastChildFill="True" Grid.Row="2">
                    <Button x:Name="AddCoverImageBtn" DockPanel.Dock="Bottom" Content="Thêm ảnh bìa" Click="AddCoverImageBtn_Click"/>
                    <Image x:Name="JourneyCoverImage"/>
                </DockPanel>
                <DockPanel LastChildFill="True" Grid.Row="3">
                    <Button Content="Thêm thành viên" Name="AddMemberBtn" Click="AddMemberBtn_Click" DockPanel.Dock="Bottom"/>
                    <ListView x:Name="MemberListView">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding MemberName}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DockPanel>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="15*"/>
                    <RowDefinition Height="56*"/>
                    <RowDefinition Height="135*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="127*"/>
                        <ColumnDefinition Width="150*"/>
                    </Grid.ColumnDefinitions>
                    <DockPanel Grid.Column="0">
                        <Label Content="Ngày đi" DockPanel.Dock="Left"/>
                        <DatePicker Grid.Row="0"
                                    HorizontalAlignment="Left"
                                    Name="DatePicker1"
                                    VerticalAlignment="Top"
                                    Width="200"
                                    FirstDayOfWeek="Monday"
                                    SelectedDateFormat="Short"
                                    />
                    </DockPanel>
                    <DockPanel Grid.Column="1" LastChildFill="True">
                        <Label Content="Ngày về" DockPanel.Dock="Left"/>
                        <DatePicker Height="25" Grid.Row="0"
                                    HorizontalAlignment="Left"
                                    Name="DatePicker2"
                                    Width="200"
                                    VerticalAlignment="Top"
                                    FirstDayOfWeek="Monday"
                                    SelectedDateFormat="Short"                                    />
                    </DockPanel>
                </Grid>
                <TextBox x:Name="JourneyDescriptionTextBox" Grid.Row="1" materialDesign:HintAssist.Hint="Mô tả chuyến đi"/>
                <DockPanel Grid.Row="2" LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top">
                        <DockPanel LastChildFill="True">
                            <TextBox x:Name="paymentValueTextBox" DockPanel.Dock="Right" Width="50" materialDesign:HintAssist.Hint="Số tiền"/>
                            <ComboBox x:Name="MemberComboBox" DockPanel.Dock="Right" Width="100">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding MemberName}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <TextBox x:Name="paymentContentTextBox" materialDesign:HintAssist.Hint="Khoản chi"/>
                        </DockPanel>
                        <Button x:Name="AddPaymentBtn" Click="AddPaymentBtn_Click" Content="Thêm khoản chi" DockPanel.Dock="Bottom"/>
                    </StackPanel>
                    <ListView x:Name="PaymentListView">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Khoản chi" DisplayMemberBinding="{Binding PaymentContent}"/>
                                <GridViewColumn Header="Người chi" DisplayMemberBinding="{Binding MemberName}"/>
                                <GridViewColumn Header="Số tiền" DisplayMemberBinding="{Binding PaymentValue}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </DockPanel>

            </Grid>
        </Grid>
    </Grid>
</Page>
